//在二维数组A中选出各行最小的元素组成一个一维数组B。
#include<stdio.h>
main(){
      int i,j,A[3][4],B[3];
  for(i=0;i<3;i++)
  for(j=0;j<4;j++)
       scanf(%d,&A[i][j]);
  for(i=0;i<3;i++){
  for(j=0;j<4;j++)
       printf(%3d,A[i][j]);
       printf(n);
  }
  for(i=0;i<3;i++)
  B[i]=A[i][0];
  for(i=0;i<3;i++){
  for(j=0;j<4;j++){
   if(B[i]>A[i][j])      B[i]=A[i][j];}
   printf(%3d,B[i]);
  }
  printf(n);
}
